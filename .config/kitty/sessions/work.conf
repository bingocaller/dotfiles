# Set the layout for the current tab
layout tall
# Set the working directory for windows in the current tab
cd ~/work/product-publisher-workflow

# Launch new windows, set their titles and run stuff in them
launch --cwd=current
launch --cwd=current
launch --cwd=current
